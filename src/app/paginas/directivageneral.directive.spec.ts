import { DirectivageneralDirective } from './directivageneral.directive';

describe('DirectivageneralDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivageneralDirective();
    expect(directive).toBeTruthy();
  });
});
